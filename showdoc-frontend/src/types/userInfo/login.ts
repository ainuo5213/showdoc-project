export interface ILoginInfo {
  token: string;
  expires: string;
  userID: number;
  username: number;
  headImg: string;
}

export interface IToken {
  token: string;
  expires: string;
  userID: number;
}
